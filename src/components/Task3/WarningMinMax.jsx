import React from 'react';
import classes from '../GeneralStyles.module.css';
const WarningMinMax = (props) => {
    return(
        <div className={classes.worning}>
            Min must be less than Max        </div>
    )
}
export default WarningMinMax;